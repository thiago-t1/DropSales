// Fonte única de verdade para configurações de ambiente.
// Altere apenas esta constante para trocar o backend entre dev e prod.
export const environment = {
  production: true,
  apiUrl: 'https://dropsales.onrender.com/api',
};
